%table.table-striped.col-md-10
  %thead
    %tr
      %th Course id
      %th Subject
      %th created date

  %tbody
    - @requests.each do |request|
      %tr
        %td= request.course_id
        %td= request.subject
        %td= request.created_at
